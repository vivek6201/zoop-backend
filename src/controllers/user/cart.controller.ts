import { Request, RequestHandler, Response } from "express";

const cartController: RequestHandler = async (
  req: Request,
  res: Response
) => {};

export const getUserCartController: RequestHandler = async (
  req: Request,
  res: Response
) => {};

export default cartController;
