import { Request, RequestHandler, Response } from "express";

const ordersController: RequestHandler = async (
  req: Request,
  res: Response
) => {};

export const getUserOrdersController: RequestHandler = async (
  req: Request,
  res: Response
) => {};

export default ordersController;
